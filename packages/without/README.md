# without

The narrow waist of the project: the contracts every plugin speaks, plus a
first-step graph recovery and mermaid visualizer.

See `without.contracts` for the typed sketch of `Processor`, `Context`,
`Stream`, and `Transition`, and `without.graph` for the `@node` decorator that
recovers a DAG from declared inputs and renders it as mermaid.
